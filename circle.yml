dependencies:
	pre:
		- echo y | android update sdk --no-ui --all --filter "Android SDK Build-Tools 25.0.2"
test:	
	override:
		- ./gradlew assembleRelease
		- cp -r project-name/build/outputs $CIRCLE_ARTIFACTS